parameters:

    paths:
        - app
        - database
        - resources/views

    # The level 9 is the highest level
    level: 5

    ignoreErrors:
    excludePaths:

    checkMissingIterableValueType: false